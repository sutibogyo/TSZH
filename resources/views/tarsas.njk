{% extends 'master' %}


{% block title %}{{ tarsas.name }}{% endblock %}
{% block content %}
{{ csrfField }}
  <h1>{{ tarsas.name }}</h1>


  <h2>Leírás</h2>
  <p>{{ tarsas.description }}</p>
  <div>
    <a class="btn btn-primary" href="{{ 'tarsas_edit' | route({id: tarsas.id}) }}">Módosítás</a>
    <a class="btn btn-danger btn-delete" href="{{ 'tarsas_delete' | route({id: tarsas.id}) }}">Törlés</a>
  </div>

{% endblock %}

